# Advisor Plans

Written against commit `49d6979` by the improve skill (focused `plan` invocation — full audit skipped at user request; scope was predetermined: shadcn/ui migration + corporate branding).

| # | Plan | Status | Depends on |
|---|------|--------|------------|
| 001 | [shadcn/ui migration + corporate branding](001-shadcn-branding.md) | DONE | — |
| 002 | [Multileg & multistage instruments](002-multileg-multistage-instruments.md) | DONE (L1+L2+L3, incl. LATEST BY) | — |
| 003 | [Materialize territory: partitioned differential dataflow](003-materialize-territory.md) | DONE (M0–M5; soak p99 0.70× vs single-grain) | superseded interim sharding idea |
| 004 | [Nested queries (non-recursive)](004-nested-queries.md) | DONE (N1–N4 + scope-aware editor autocomplete) | — |
| 005 | [Dapr sibling runtime (shared core + polyglot)](005-dapr-port.md) | DONE (W0–W9; original scoreboard Dapr p50 7ms / Orleans p50 122ms was later root-caused to Orleans' pull-based memory streams, not the actor model — with the post-plan `--Streams:Transport push` bus Orleans measures p50 1ms; see comparison.html and plan status addenda) | 001–004 (ports the platform they built) |
| 006 | [Ingestion connectors + config import/export](006-connectors-and-config.md) | DONE (W0–W7; four real connector kinds + config import/export live on both flavors; headline cross-flavor federation — Dapr subscribed an Orleans table by id over gRPC, real rows flowing) | 005 (shared core + both flavors) |
| 007 | [Containers + Cloud Run prep, admin app, AI control chat](007-cloudrun-admin-aichat.md) | DONE (both flavors containerized + Cloud Run-ready; admin app drove both stacks live; Gemini AI control chat on both flavors with audit-trace UI) | 005, 006 |

| 008 | [REST discoverability, table joins, set operations, ingress, lineage](008-rest-joins-setops-ingress-lineage.md) | DONE (W1–W5 landed) | 005, 006, 007 |
| 009 | [Ingress hardening, NATS transport, table delta journal](009-ingress-hardening-nats-journal.md) | DONE (A1–A2, B1–B3, C1–C2, D landed; NATS verified end to end against a real broker) | 008 |
| 010 | [Pluggable ingress/egress transports](010-pluggable-transports.md) | DONE (a transport is now one class + one registry line; recipe in [TRANSPORTS.md](../TRANSPORTS.md)) | 009 |
| 011 | [Lineage edges, SQL editor UX, memory stability, sharded tables](011-lineage-sql-memory-sharding.md) | DONE (lineage edges restored; SQL revert/format; the flush amplifier fixed 287→109 MB/min and row retention plateaus a table; per-key shards shipped but **measured NOT to be a memory optimisation** — query locality and swap-out only; per-entity Scalar docs) | 010 |
| 012 | [CSV in, CSV out](012-csv-io.md) | DONE (url sources read CSV/NDJSON; TSV/semicolon/pipe detected; a `file` sink kind writes CSV or NDJSON; CSV download for table rows and pipeline results) | 010 |
| 013 | [`sf` admin CLI + MCP server](013-admin-cli-mcp.md) | DONE (`bun admin/sf.ts` for a terminal, `bun admin/mcp.ts` for an agent — one shared REST client, zero npm deps, MCP stdio hand-written to the spec and pinned by subprocess conformance tests) | 012 |
| 014 | [Pluggable connectors + database ingress/egress](014-pluggable-connectors-databases.md) | DONE (waves A–M + L·Docs; `IPolledTransport` + a durable cursor, `postgres`/`mssql` source and sink kinds, CDC-envelope unwrapping, `INSERT INTO` sugar, live-DB suite gated on Docker) | 013 |
| 015 | [RBAC → entitlements, groups, approvals, escalation, audit](015-rbac-entitlements-approvals.md) | DONE (waves 1–8; per-resource grants resolved server-side per request — never from the JWT — so a revocation lands within `Auth:PolicyCacheSeconds`; `Auth:Mode=legacy` is the one-flag rollback and a typo in it leaves enforcement ON; optional approvals that gate and then actually execute; a day-sharded audit log whose `truncated` counter exists so silence is never read as absence) | independent of 014 |
| 016 | [Name resolution, versioning & dependencies, service discovery](016-identity-versioning-discovery.md) | DONE — waves 0–7 landed (`EntityRef` id-or-name resolution; `SchemaCompatibility` reusing `FieldNumberMap`; real `Revision`/`SchemaRevision` with dependent-schema refresh so `/proto` stops serving a stale shape; `dependsOn` pinning reachable over REST *and* config import; `SqlCompiler.ExtractReferences` replacing the FROM-only regex; fatal import cycles + the `schemaPolicy` gate; connector version requirements; and the headline — `GET /api/meta/instance`, a configured peer directory, and a federated `grpc` source that dials a **peer by name** and an **entity by name**, verified live between two instances with no address and no GUID anywhere; and `@name` external endpoints resolved at connect time and never written back, so one unedited catalog ingested different data under two different `Endpoints:` maps). Docs, an `sf-federate` skill, and a late fix for the one thing the docs wave caught the plan overclaiming — `mode=validate` really does report a broken `dependsOn` pin now. Per-wave outcomes and the deliberately-deferred list are at the end of the plan document | 014 (plugin versions only) |
| 017 | [Native .NET CDC](017-native-cdc.md) | DONE (waves A–G; `postgres-cdc` reads logical replication, `mssql-cdc` reads capture tables, `CdcPreflight` reports the operational hazards before a source is turned on; wave G's live-DB suite gated on Docker) | 014 (reuses `IPolledTransport`, `DbSourceConfig`, `CdcEnvelope`'s vocabulary) |
| 018 | [FIX protocol: the `fix` format + a receive-only session source](018-fix-connector.md) | DONE (the `fix` format — tag=value, dictionary-free, repeating groups as nested arrays — usable by url/file/folder/nats sources; a receive-only FIX session source in `shared/StreamsForge.Connectors.Fix` on QuickFIXn.Core 1.14.1; live check on an isolated instance confirmed rows flowing from a real QuickFIX/n acceptor into a table) | 010 (`IInboundTransport`), 014 (`Text` field type, out-of-core project precedent) |
| 019 | [FIX order entry: a first-class bidirectional connector](019-fix-order-entry.md) | DONE (`fix-duplex`, `IDuplexTransport` — the third transport seam; a source owns the live session, a stateless `duplex` sink on any pipeline/table reaches it by name so a sink field edit never re-logs-on; a live check sent orders through a real acceptor and proved the drop-copy match with the platform's own SQL, joined on `ClOrdID`) | 018 (`fix` format + session project), 010, 014 |
| 020 | [CRDT edge sync: offline-tolerant ingress on Yjs](020-crdt-edge-sync.md) | DONE (Orleans) — all waves A–G landed: a `crdt` source kind whose grain owns a Yjs document and projects it to rows, verified live — a document edited entirely offline converges on reconnect, redelivery emits zero rows (D7) even across a restart, and a deleted key genuinely frees itself from a `LATEST BY` table. That last one needed a third stamp, `_retract`: `_weight` alone reaches a sink but never converges a table, a limit CDC has always had too. Wave C found the acceptance criterion it was written to prove was FALSE: after a `kill -9` the document came back intact and the table came back **empty and stuck rebuilding** — `TableGrain` rebuilds a resuming table "purely from live traffic", and D7 guarantees a document has none. Fixed with a re-assert (`ReplayAsync`, issued after every table-resume pass and via `POST /api/sources/{name}/crdt/replay`); the same defect hits a table created over an already-populated document, no restart needed. Wave A: `external/ycs` is a **submodule** pinned to the fork's `parity-yjs-13.6.32` branch (D10's GitHub-Packages pin overruled — no package exists to consume), with 4 pin tests including D7 idempotence. Originally: a `crdt` source kind whose grain owns a Yjs document and projects it to rows; the dataflow stays CRDT-unaware. New external dependency (the `cotyar/ycs` fork), and wave D is blocked on 015 | 010, 014; wave D on 015 |
| 021 | [Environment isolation: non-overlapping catalogs inside one server](021-environment-isolation.md) | DONE on Orleans (waves 0–3, docs included; the plan's cross-flavour live check against Dapr is still open — see [`dapr/PARITY.md`](../dapr/PARITY.md); an environment is the registry KEY — a whole separate `RegistryGrain`/`RegistryActor` per environment, not a filtered column; `default` is the empty string internally, so `EnvKeys.Qualify("", k) == k` and every pre-existing key is byte-identical, no migration; separator is `.`, overruling the plan's own D3 `:`; seeding is default-environment-only; a namespace, **not** a security boundary until 015; found the Dapr host unbootable since plan 009 and fixed it here) | nothing hard; cheaper after 016-A, scoped by 015 |
| 022 | [Server plugins & single-file publish](022-plugins-and-single-file.md) | Waves 0–1 DONE, wave 2 (Crdt plugin) landing now (Quant, Fix and — Orleans only — Crdt ship as install-time server plugins under `plugins/`, each ILRepack-merged to one DLL; `IStreamsForgePlugin`/`IStreamsForgeWebPlugin` are the two host hooks; both hosts publish as a single self-contained native executable via `tools/publish.sh`, no trimming/AOT, SPA/docs/protos embedded with disk-first fallback; full guide in [`PLUGINS.md`](../PLUGINS.md)) | nothing hard |
| 023 | [Source stability (no lost rows) and plugin hooks](023-source-stability-and-plugin-hooks.md) | DONE on Orleans (folder per-file isolation — good files next to a bad one used to be dropped forever; the poll cycle always re-arms; a late table/pipeline replays the source's recent rows through an attach gate; consumers resume before producers, once per activation; the console tape paces instead of drops and learns of a new source at once; exact-count tests for file/folder/url, a two-host folder→gRPC→table chain (1000/1000) and a host-restart test in `StreamsForge.Chain.Tests`; UI plugins get `draft`/`onSuggest` (apiVersion 3), `.ts/.tsx` via lazy in-browser sucrase, `?v=` cache-busting; the plugin loader loads-then-scans and names version conflicts; Dapr owes three items, PARITY D6) | 022, 010 |
| 024 | [TLS: native HTTPS/gRPC-TLS, outbound trust, every client SDK](024-tls-support.md) | DONE on Orleans (`Tls:Enabled` + `Kestrel:Certificates:Default` put TLS on both listeners with fail-fast startup; HSTS; `ASPNETCORE_FORWARDEDHEADERS_ENABLED` behind a proxy; `Tls:TrustedCaPath`/`Tls:AcceptAnyCertificate` for every outbound HTTPS/gRPC call; NATS `tls` group; `https://` gRPC targets + CA/skip-verify options in the TypeScript, .NET, Python and Kotlin SDKs and the admin CLI; `tools/tls/dev-cert.sh`; proven by `TlsHostTests`, `TlsChainTests` (two hosts over TLS, 200/200), `ForwardedHeadersTests` and one live TLS suite per SDK; also fixed plan 022's bare-publish-is-linux-x64 default that broke every client fixture on macOS. Dapr out of scope) | 022, 016 |
| 025 | [Dapr parity with Orleans](025-dapr-parity.md) | DONE — see the plan's own outcomes section for what has actually landed and been verified live, not this row. Scope: the seven gRPC services move to `shared/StreamsForge.Api/Grpc/` behind a new `IEntityStreamFacade` so the Dapr host serves gRPC on `:5499` too; TLS on the Dapr host mirrors plan 024; late-consumer replay, coordinated boot order, paced source relay, table-over-pipeline and an honest `crdt` Failed status all port from Orleans to Dapr; a new isolated Dapr test instance (app-id `streamsforge-dapr-test`, Redis DB 1, dedicated placement container) makes live Dapr verification possible for the first time since plan 009 broke it silently for 164 commits (see `dapr/PARITY.md`) | 024, 023, 021, 020, 016 |
| 026 | [Stream replay: subscribe from a position in the past](026-stream-replay.md) | PLANNED (producer-owned sequence-numbered `ReplayLog` generalizing plan 023's ring across all five publish sites; `from` seq/timestamp on the attach gate, gRPC and SignalR; consumer-side `replayFrom` applied at start only, because replay into a live executor is late events by construction; an opt-in persisted NDJSON segment log with retention that survives a restart independently of `TablePersistenceMode` and lets a table rebuild operator state from its inputs' logs; SDKs + console; Orleans first, Dapr owes PARITY lines) | 023, 025 |

Design tokens source of truth: `orleans/design-system/streamsforge/MASTER.md`.
